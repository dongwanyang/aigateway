# Window Code Review — Code RAG folder 上传 + 函数/类计数修复

**范围**：本窗口 9 个文件（排除其他窗口的 e2e/test_debug_admin 等改动）。
- `aigateway-api/requirements.txt`（+python-multipart）
- `aigateway-api/src/aigateway_api/code_rag_routes.py`（duck-typing UploadFile / list_code_tasks bytes 解码 / 404）
- `aigateway-core/.../code_rag/graph_builder.py`（symlink 到可写 work_dir）
- `aigateway-core/.../code_rag/graph_query.py`（后缀路径归一化 + BFS 前缀剥离）
- `control-panel/nginx.conf`（client_max_body_size 256m + 代理超时）
- `control-panel/src/api/client.ts`（FormData Content-Type 修复 / detail 解析）
- `control-panel/src/pages/KnowledgeCodeTab.tsx`（dismissTask 去重 / 404 处理）
- `tests/test_code_rag_helpers.py`、`tests/test_code_rag_routes.py`（回归测试）

## 循环结果

- **Round 1**: 发现 3 个问题（1 correctness: `_normalize` startswith 脆弱前缀；1 test-coverage: 测试名误导；1 simplification: 4 空行）。全部修复。
- **Round 2**: zero findings → **early exit**（clean pass）。

### Round 1 修复明细
1. `graph_query.py` `lookup_related_symbols_strict`：删掉无分隔符保护的 `startswith(file_path)` 分支，统一只用 `endswith("/" + file_path)`，避免 `auth.py` 误当前缀匹配 `auth.py_backup`。
2. `test_code_rag_routes.py`：`test_get_code_task_returns_default_shape_when_missing` → `test_get_code_task_returns_404_when_missing`（名字与断言一致）。
3. `test_code_rag_routes.py`：清理 zip 测试后的 4 个连续空行 → 2 个。

## 端到端验证（rebuild 后）
- `curl localhost:8000/health` → 200 healthy，日志无 error。
- 经 nginx :3000 multipart folder 上传 → HTTP 200 + task_id（原 3 个用户报错 UTF-8/413/JSON-parse + 后续 500 全部消失）。
- folder 导入完成：`function_count=1, class_count=1`（2 文件）。
- 重新导入 aigateway-core（578 chunks）：`function_count=108, class_count=82`（之前显示 0 是陈旧数据）。
- `tests/test_code_rag_routes.py` + `tests/test_code_rag_helpers.py`：44 passed, 1 skipped。

## 结论
Code review loop complete (2 rounds, early exit on Round 2 clean).
- Round 1: Fixed 3 issues (Critical: 0, Important: 1, Minor: 2)
- Round 2: zero findings → early exit
- All changes in working tree, unstaged. Ready for user review.
