# ============================================================
# AI Gateway — Python 依赖清单(合并自原 Dockerfile 的 6 个 pip 层)
# ============================================================
# 新增/升级依赖请改本文件,而非 Dockerfile。
# torch 单独在 Dockerfile 中安装(CPU 版,从前置层稳定缓存)。
# ============================================================

# --- Web 框架与服务 ---
fastapi
uvicorn[standard]
gunicorn
pyyaml
watchdog
prometheus-client
structlog
cachetools
lz4
httpx
redis
hiredis
pydantic
litellm
Pillow

# --- 多模态处理 ---
pytesseract
opencv-python-headless
pymupdf
python-docx
beautifulsoup4

# --- Embedding / 语义缓存(transformers,torch 在 Dockerfile 单独装)---
transformers>=4.51.0
sentence-transformers

# --- Prompt 压缩 ---
llmlingua>=0.2.0

# --- Qdrant 客户端(RAG 存储) ---
qdrant-client

# --- 配置加载:.env 文件支持 ---
python-dotenv
