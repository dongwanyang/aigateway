# Optional GPU override
# Usage:
# docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d

services:
  gateway:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
